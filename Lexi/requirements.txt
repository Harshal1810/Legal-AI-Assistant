# ── Core LangChain stack ──────────────────────────────────────────────
langchain>=0.3.0
langchain-core>=0.3.0
langchain-groq>=0.2.0        # ChatGroq with json_mode structured output

# ── LLM provider ─────────────────────────────────────────────────────
groq>=0.9.0

# ── PDF parsing ───────────────────────────────────────────────────────
pdfplumber>=0.11.0            # better layout than PyPDF2 for legal docs

# ── Data validation ───────────────────────────────────────────────────
pydantic>=2.0.0               # JudgmentMetadata schema

# ── Retry / resilience ────────────────────────────────────────────────
tenacity>=8.2.0               # exponential backoff for API rate limits

# ── Retrieval (used later by the retriever module) ────────────────────
rank-bm25>=0.2.2              # BM25Okapi for keyword retrieval
sentence-transformers>=3.0.0  # local embeddings (BAAI/bge-base-en-v1.5)
                              # + cross-encoder reranking

# ── Vector store ──────────────────────────────────────────────────────
chromadb>=0.5.0               # persistent local vector store

# ── LangGraph (agent orchestration — installed separately) ────────────
langgraph>=0.2.0